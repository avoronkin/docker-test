{
  "name": "docker-nodejs-test",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build-docker": "sudo docker build -t avoronkin/nodejs .",
    "docker": "sudo docker run -v $(pwd):/src -p 127.0.0.1:3001:3001 -i -t avoronkin/nodejs /bin/bash"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.11.0"
  }
}
